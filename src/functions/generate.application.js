import ora from "ora";
import path from "path";
import chalk from "chalk";
import fs from "fs/promises";
import { execSync } from "child_process";
import { promisify } from "util";

export async function generateNewApplicationWithNpm(projectName) {
    try {
    } catch (error) {}
}
export async function generateNewApplicationWithPnpm(projectName) {
    try {
    } catch (error) {}
}
export async function generateNewApplicationWithYarn(projectName) {
    try {
    } catch (error) {}
}
